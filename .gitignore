# Dataset — large binary/data files, not tracked in git
dataset/
dataset.zip

# Large slide deck — exceeds GitHub 100MB single-file limit
*.pptx

# Prebuilt indexes — rebuilt from dataset on the box with the embedding
# endpoint; large binary, never tracked (GitHub 100MB single-file limit)
reference_submission/index/

# Run outputs — review.jsonl is regenerated each run; human-labeled
# copies kept under output_human_gold/. The curated output/generate/*.md
# samples stay tracked (already committed).
output/review.jsonl
output_human_gold/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Env — .env IS tracked (private repo; cloud eval pulls config from git)

# Local-only smoke-test deps (eval box installs from requirements.txt)
.deps/

# Provided resources — injected by the cloud/eval env, not our code
# (problem set incl. ground truth, and baseline reference prompts)
# reference_submission/problem/
# reference_submission/prompt_templates/reference_generate.md
# reference_submission/prompt_templates/reference_review.md

# IDE
.idea/
.vscode/

generatedataset/

python_env/